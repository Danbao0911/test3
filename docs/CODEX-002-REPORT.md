# CODEX-002 · 商务联系候选、证据核验与账号工作台

> R1 第二部分审查整改以 [`CODEX-002-R1-REPORT.md`](./CODEX-002-R1-REPORT.md) 为准；T05 账号工作台以 [`CODEX-002-T05-R1-REPORT.md`](./CODEX-002-T05-R1-REPORT.md) 为准；T06 初始交付以 [`CODEX-002-T06-REPORT.md`](./CODEX-002-T06-REPORT.md) 为准，最新证据/有效性/撤销整改以 [`CODEX-002-T06-R1-REPORT.md`](./CODEX-002-T06-R1-REPORT.md) 为准；T07 受控导出、删除与审计以 [`CODEX-002-T07-REPORT.md`](./CODEX-002-T07-REPORT.md) 为准。均追加到 [PR #2](https://github.com/Danbao0911/test3/pull/2)。

范围：任务书 T04，并补齐其依赖的 T01 三角色/提取许可及 T02 证据、候选、审核关系。基于 `codex/001-account-import` 的 `b8a810f`，在 `codex/002-contact-review` 增量开发。[PR #2](https://github.com/Danbao0911/test3/pull/2) 已创建；PR #1 尚未合并，本轮 PR 以该功能分支为基线，不修改 main、不自动合并、不部署生产。

## 后续 T06 状态

T06 初始去重与跨账号关联审核已追加到同一 PR；T06-R1 已在该分支补齐关系专用证据、当前有效性、稳定加锁、不可变决定历史、网页撤销、分页建议和请求体边界。详见 [`CODEX-002-T06-R1-REPORT.md`](./CODEX-002-T06-R1-REPORT.md)。最终实现与测试 SHA 为 `ee2a49838a9b75eb5ef4f225f3d856252b954b68`，Push CI `35325485260` 与 PR CI `35325489318` 均通过；unit 99、integration 47、E2E 7，失败/跳过均为 0。该绿色 CI 仅证明隔离 CI 验证通过，不代表生产上线。

## T07 状态

T07 已在同一分支完成受控 CSV 导出、字段级来源策略校验、加密一次性到期链接、最小导出审计、拒绝联系 HMAC 抑制、账号/联系项删除、临时导出清理和到期清理。详见 [`CODEX-002-T07-REPORT.md`](./CODEX-002-T07-REPORT.md)。功能最终 SHA 为 `1437a594fedb72f1cbe63b96a6778d2ac3ef4bb4`；最终 Push CI `35330985799` 与 PR CI `35330990951` 均通过；unit 102、integration 50、E2E 8，失败/跳过均为 0。T07 仍只在隔离 demo/test 环境验证，不代表真实数据或生产上线。

## 已实现

- ADMIN、REVIEWER、VIEWER：来源策略仅管理员可改；审核员可录入/提取/核验；只读成员不能写入，联系列表和详情均使用白名单 DTO，隐藏原值、规范值、证据正文、证据地址、字段位置、审核原因。
- 来源独立的 allowExtract / allowEvidenceText、有效天数、策略版本和操作人；能力默认关闭。仅批准账号录入并不能处理联系值。
- 五种规则字段：商务邮箱、明确商务微信、企业电话、官网联系页、预约链接。标签不明确、格式不符、缺失或第三方上下文返回零候选，不猜测。无网页抓取、拨号、邮箱发送验证或外部模型调用。
- 每条候选有独立最小证据片段（最多 300 字）、字段定位、来源 HTTPS 地址、取得时间和策略版本。完整输入不存储。请求流上限 32 KiB、文本 5000 字、最多 20 项。
- 待审核 → 通过/驳回/失效，批准须同时确认归属及商务用途。已驳回/失效不能直接恢复。历史记录包括操作人、决定、原因、时间和版本。
- 相同账号/来源版本/类型/规范值精确去重，重复提取不重建候选，不复活驳回项；不同账号不因共享公司邮箱合并。
- 来源行锁与来源修改共用；审核额外锁联系项并校验版本。并发审核一个成功、一个 409，不覆盖另一审核结果。读取/批准时重新计算来源、策略及期限，旧批准状态不等于当前可用。
- 最小化审计仅存 actor/action/target/time，不复制联系值、原文或审核原因。
- 中文来源策略表单、账号商务联系页、带分页及审核状态筛选的审核工作台、成员角色展示、受控命令行成员创建。

## 安全前置与范围限制

联系处理只开放隔离 demo/test 环境。生产提取和批准主动返回 403，原因是 T07 物理删除、抑制和到期清理尚未交付，不能提前接受真实联系人。

仅允许同一账号已登记来源；只支持可保存文本的证据。未获准保留文本的来源拒绝处理，不以哈希代替证据。演示候选仅接受示例域邮箱/链接、demo_ 微信、+1 202 555 01xx 电话；演示主页拒绝真实平台地址。既有数据库记录不自动改写或合并。

当前“有效天数”限制可用性，并由 T07 到期清理任务在隔离环境物理清理过期联系、导出和抑制。输入证据不会自动验证页面所有权；人工确认仍是必要步骤。规则较保守，未识别信息不等于不存在。联系失效/驳回当前不可逆，同一策略版本不会自动刷新过期证据；修正需新来源策略版本和新取得的证据。T05 工作台、T06 关联审核和 T07 受控导出/删除/抑制已交付；四平台真实采集和生产外联仍保持关闭。

## 数据库变更及回滚

新增迁移 `20260918120000_contact_review`：UserRole、ContactType、ContactStatus 枚举，来源策略字段，Evidence / ContactPoint / ReviewDecision / AuditEvent 表，以及唯一键、外键和 CHECK 约束。ContactPoint 必须绑定存在的证据；APPROVED 必须有归属/商务确认及审核时间。旧版本仅有的管理员账户保留 ADMIN，新建默认 VIEWER。

没有删除旧表/旧列或转换账号身份键。升级先备份，在隔离库验证后执行 `pnpm db:generate && pnpm db:migrate`。本次未对用户开发库执行迁移或清空操作。

推荐前向修复。不得把旧代码直接对外运行在新多角色库上，旧版本没有角色检查。紧急回滚应停服务、保存当前证据及审核记录，恢复升级前已验证的整库备份和匹配代码；恢复会丢失备份后的写入，须由负责人明确授权。不提供自动删表降级命令，本轮没有执行备份恢复演练。

## 验证记录

代码/测试提交：`9f53a1a`（实现）、`163122a`（HTTP/E2E及非特权测试角色）、`06e70c1`（测试库迁移权限与演示输入约束）、`04629e6a7c7411ccd1977a976f45ab8578d60ff8`（上下文规则修复及运行说明）。均已推送 GitHub。

本地已运行：`pnpm db:generate`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`，退出码均为 0；单元测试 88 通过、0 失败、0 跳过。提取用例含 24 正向 + 38 缺失/第三方/歧义/无效输入，另有上下文、权限、期限、精确去重与无外联断言。

本机没有可用 Docker/PostgreSQL，未在本机运行数据库迁移、集成或浏览器测试。`pnpm build` 本机两次失败（exit 1）：Turbopack 创建内部进程绑定端口时被运行环境禁止，申请沙箱外执行后仍受限；这不记为通过。

CI 首次基础实现 [35260493120](https://github.com/Danbao0911/test3/actions/runs/35260493120) 通过，但该提交尚未包括新增 HTTP/E2E 验收，不能作为新功能完整验证证据。

CI [35261298472](https://github.com/Danbao0911/test3/actions/runs/35261298472) 实际失败：非特权角色能创建表，但旧迁移 `CREATE SCHEMA IF NOT EXISTS` 仍需要本轮数据库级 CREATE 权限；后续迁移、集成及 E2E 未运行。`06e70c1` 只向临时测试库授予 CONNECT/CREATE，未授予超级用户或角色级 CREATEDB。

CI [35261557937](https://github.com/Danbao0911/test3/actions/runs/35261557937) 实际执行迁移成功、HTTP 28 通过/1 失败、浏览器 2 通过。失败用例 C09 揭示地址内部的 `invalid` 被误当上下文，返回零候选而未进入演示地址限制；修复为区分标签/外围上下文与地址值，并增加相应回归及跨行第三方标记测试，没有放宽断言。

最终完整 CI：[35261994987](https://github.com/Danbao0911/test3/actions/runs/35261994987)，对应完整 SHA `04629e6a7c7411ccd1977a976f45ab8578d60ff8`，verify / e2e 两个 job 全部成功，无测试跳过，无自动重试。

| 实际命令（CI） | 结果 | 退出码 |
| --- | --- | --- |
| pnpm install --frozen-lockfile | 成功 | 0 |
| pnpm db:generate | 成功 | 0 |
| pnpm test:unit | 88 通过，0 失败，0 跳过 | 0 |
| pnpm exec tsx scripts/prepare-ci-database.ts | 两个独立临时数据库的非特权角色初始化成功 | 0 |
| pnpm db:migrate | 空库按顺序执行 3 个迁移成功 | 0 |
| pnpm lint / pnpm typecheck | 均成功 | 0 |
| pnpm test:integration | 29 通过，0 失败，0 跳过（含新增 C01–C14） | 0 |
| pnpm setup:local / pnpm admin:create | 临时 E2E 管理员初始化成功 | 0 |
| pnpm exec playwright install --with-deps chromium | 成功 | 0 |
| pnpm test:e2e | 2 通过，0 失败，0 跳过，重试数 0 | 0 |
| pnpm build | CI 正式构建成功；本机受限失败另列于上文 | 0 |

没有生成或提交演示截图，不以页面截图替代数据库断言。未运行：真实数据试点、四平台真实调用、物理清理/备份恢复演练、本机迁移/浏览器和本次改动后的 demo seed 实库执行。

## 验收映射

| 用例 | 实际断言 |
| --- | --- |
| C01–C03 | 独立提取许可；未授权不落库；五类候选独立证据；重复/并发精确去重 |
| C04–C07 | 双确认/原因必填；审核员权限；只读 API 脱敏和八类写入拒绝；并发版本冲突；失效/驳回历史及不复活 |
| C08–C10 | 空信息/第三方零候选；无效请求/期限/地址/真实值/来源匹配；数据库证据外键及批准确认约束 |
| C11–C14 | 到期不可批准；最小化审计；策略变化即时失效；撤销并发顺序；过期来源拒绝；401/CSRF/404 |
| 既有 T01–T15 | 真实 HTTP 账号导入回归；首次 90 行 60/20/10，二次新 key 0/80/10；没有把旧测试覆盖范围扩大表述 |
| 浏览器 1 | 登录—唯一来源批准—独立夹具 90 行导入—平台/来源筛选—详情 |
| 浏览器 2 | 独立来源提取授权—单条账号—两类候选—证据—双确认—审核筛选—失效—来源撤销后隐藏 |

CI 两个 job 使用不同绑定 run ID 的 PostgreSQL。迁移前先跑保护单测，测试连接后核验非特权角色，清理仅限本轮用户/来源/批次。E2E 配置在启动服务前校验，不复用既有服务器、不自动重试掩盖失败，临时密码在 Actions 中掩码。

下一阶段：T08/T09 平台能力与授权验证；在真实数据试点前继续保持生产联系人提取/批准、平台爬虫和外联关闭。T07 的备份恢复演练及外部导出副本回收仍需在受控环境另行验证。
