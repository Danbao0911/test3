"use client";

import Link from "next/link";
import { FormEvent, useCallback, useEffect, useState } from "react";

const labels: Record<string, string> = { XIAOHONGSHU: "小红书", YOUTUBE: "YouTube", X: "X", DOUYIN: "抖音" };
const followUpLabels: Record<string, string> = { NOT_CONTACTED: "未联系", CONTACTING: "人工联系中", REPLIED: "已回复", NOT_MATCH: "不匹配", DO_NOT_CONTACT: "不再联系" };
type Account = { id: string; platform: string; nativeId: string | null; displayName: string; profileUrl: string; normalizedProfileUrl: string; organization: string | null; serviceTags: string[]; region: string | null; sourceId: string; sourceUrl: string; capturedAt: string; createdAt: string; updatedAt: string; isDemo: boolean; favorite: boolean; owner: { id: string; email: string; role: string } | null; followUp: { status: string; note: string; updatedAt: string | null; updatedById: string | null }; reviewStatus: string | null; hasUsableContact: boolean; source: { name: string; type: string; status: string; allowImport: boolean; permissionNote: string; expiresAt: string | null } };
type User = { id: string; email: string; role: string };

export function AccountDetail({ id, canEdit }: { id: string; canEdit: boolean }) {
  const [account, setAccount] = useState<Account | null>(null); const [form, setForm] = useState({ displayName: "", organization: "", serviceTags: "", region: "" }); const [workspaceForm, setWorkspaceForm] = useState({ ownerId: "", status: "NOT_CONTACTED", note: "" }); const [users, setUsers] = useState<User[]>([]); const [error, setError] = useState(""); const [saved, setSaved] = useState(false); const [workspaceSaved, setWorkspaceSaved] = useState(false);
  const loadAccount = useCallback(async () => { const response = await fetch(`/api/accounts/${id}`); const data = await response.json(); if (!response.ok) { setError(data.message ?? "账号加载失败"); return; } setAccount(data.item); setForm({ displayName: data.item.displayName, organization: data.item.organization ?? "", serviceTags: data.item.serviceTags.join("|"), region: data.item.region ?? "" }); setWorkspaceForm({ ownerId: data.item.owner?.id ?? "", status: data.item.followUp.status, note: data.item.followUp.note }); }, [id]);
  useEffect(() => { queueMicrotask(() => { void loadAccount(); }); }, [loadAccount]);
  useEffect(() => { if (canEdit) void fetch("/api/users").then(async (response) => { if (response.ok) setUsers((await response.json()).items); }); }, [canEdit]);
  async function save(event: FormEvent) { event.preventDefault(); setError(""); setSaved(false); const response = await fetch(`/api/accounts/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json", Origin: window.location.origin }, body: JSON.stringify({ displayName: form.displayName, organization: form.organization || null, serviceTags: form.serviceTags.split("|").map((tag) => tag.trim()).filter(Boolean), region: form.region || null }) }); const data = await response.json(); if (!response.ok) { setError(data.message ?? "保存失败"); return; } setAccount(data.item); setSaved(true); }
  async function toggleFavorite() { if (!account) return; setError(""); const response = await fetch(`/api/accounts/${id}/favorite`, { method: account.favorite ? "DELETE" : "POST", headers: { Origin: window.location.origin } }); const data = await response.json(); if (!response.ok) { setError(data.message ?? "收藏状态更新失败"); return; } setAccount({ ...account, favorite: data.favorite }); }
  async function saveWorkspace(event: FormEvent) { event.preventDefault(); if (!account) return; setError(""); setWorkspaceSaved(false); const currentOwnerId = account.owner?.id ?? ""; if (workspaceForm.ownerId !== currentOwnerId) { const ownerResponse = await fetch(`/api/accounts/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json", Origin: window.location.origin }, body: JSON.stringify({ ownerId: workspaceForm.ownerId || null }) }); if (!ownerResponse.ok) { const data = await ownerResponse.json(); setError(data.message ?? "负责人保存失败"); return; } } if (workspaceForm.status !== account.followUp.status || workspaceForm.note !== account.followUp.note) { const followResponse = await fetch(`/api/accounts/${id}/follow-up`, { method: "PATCH", headers: { "Content-Type": "application/json", Origin: window.location.origin }, body: JSON.stringify({ status: workspaceForm.status, note: workspaceForm.note }) }); if (!followResponse.ok) { const data = await followResponse.json(); setError(data.message ?? "跟进记录保存失败"); return; } } await loadAccount(); setWorkspaceSaved(true); }
  if (error && !account) return <main className="page"><div className="notice error">{error}</div><Link className="text-link" href="/accounts">返回账号库</Link></main>;
  if (!account) return <main className="page"><div className="empty">正在加载账号…</div></main>;
  return <main className="page"><div className="page-header"><div><h1 className="page-title">{account.displayName}</h1><p className="page-subtitle">账号详情 · {labels[account.platform] ?? account.platform}{account.isDemo ? " · 演示数据" : ""}</p></div><div className="inline-actions"><Link className="button" href={`/accounts/${id}/contacts`}>商务联系与证据</Link><Link className="button secondary" href="/accounts">返回账号库</Link></div></div><section className="card" style={{ marginBottom: 18 }}><dl className="detail-grid"><div className="detail-item"><dt>平台账号 ID</dt><dd>{account.nativeId || "未提供"}</dd></div><div className="detail-item"><dt>数据来源</dt><dd>{account.source.name} <span className={`badge ${account.source.status === "APPROVED" ? "success" : "warning"}`}>{account.source.status}</span></dd></div><div className="detail-item"><dt>主页链接</dt><dd><a className="text-link" href={account.profileUrl} target="_blank" rel="noreferrer noopener">主动打开主页 ↗</a></dd></div><div className="detail-item"><dt>来源证据链接</dt><dd><a className="text-link" href={account.sourceUrl} target="_blank" rel="noreferrer noopener">主动打开来源 ↗</a></dd></div><div className="detail-item"><dt>服务标签</dt><dd><div className="tag-list">{account.serviceTags.length ? account.serviceTags.map((tag) => <span className="tag" key={tag}>{tag}</span>) : "未填写"}</div></dd></div><div className="detail-item"><dt>公开服务地区</dt><dd>{account.region || "未填写"}</dd></div><div className="detail-item"><dt>联系方式</dt><dd>{account.hasUsableContact ? <span className="badge success">有当前可用联系方式</span> : "暂无当前可用联系方式"}</dd></div><div className="detail-item"><dt>录入时间</dt><dd>{new Date(account.createdAt).toLocaleString("zh-CN")}</dd></div><div className="detail-item"><dt>捕获时间</dt><dd>{new Date(account.capturedAt).toLocaleString("zh-CN")}</dd></div></dl></section><section className="card" style={{ marginBottom: 18 }}><div className="page-header" style={{ marginBottom: 12 }}><div><h2 style={{ margin: 0, fontSize: 18 }}>收藏与人工跟进</h2><p className="muted small">跟进状态只记录人工行为，不代表系统已经发出邮件或私信。</p></div>{canEdit ? <button className="button secondary" type="button" onClick={() => void toggleFavorite()}>{account.favorite ? "取消收藏" : "收藏账号"}</button> : <span className="badge neutral">{account.favorite ? "已收藏" : "未收藏"}</span>}</div><form className="form-grid" onSubmit={saveWorkspace}><div className="field"><label htmlFor="owner">负责人</label>{canEdit ? <select id="owner" value={workspaceForm.ownerId} onChange={(e) => setWorkspaceForm({ ...workspaceForm, ownerId: e.target.value })}><option value="">未分配</option>{users.map((item) => <option key={item.id} value={item.id}>{item.email} · {item.role}</option>)}</select> : <div className="detail-item">{account.owner?.email ?? "未分配"}</div>}</div><div className="field"><label htmlFor="followUpStatus">跟进状态</label>{canEdit ? <select id="followUpStatus" value={workspaceForm.status} onChange={(e) => setWorkspaceForm({ ...workspaceForm, status: e.target.value })}>{Object.entries(followUpLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select> : <div className="detail-item">{followUpLabels[account.followUp.status] ?? account.followUp.status}</div>}</div><div className="field full"><label htmlFor="followUpNote">跟进备注</label>{canEdit ? <textarea id="followUpNote" maxLength={1000} value={workspaceForm.note} onChange={(e) => setWorkspaceForm({ ...workspaceForm, note: e.target.value })} placeholder="仅记录人工跟进事实和下一步，不填写密码或完整联系人原文" /> : <div className="detail-item pre-wrap">{account.followUp.note || "暂无备注"}</div>}</div>{canEdit && <><div className="muted small full">当前状态：{followUpLabels[workspaceForm.status] ?? workspaceForm.status}</div>{error ? <div className="notice error full">{error}</div> : null}{workspaceSaved ? <div className="notice success full">收藏、负责人和跟进状态已保存。</div> : null}<div className="form-actions full"><button className="button">保存工作台状态</button></div></>}</form></section>{canEdit ? <section className="card"><h2 style={{ marginTop: 0, fontSize: 18 }}>维护业务资料</h2><p className="muted small">本轮只允许修改展示资料，不允许通过编辑更换平台身份或数据来源。</p><form className="form-grid" onSubmit={save}><div className="field"><label htmlFor="displayName">账号名称</label><input id="displayName" value={form.displayName} onChange={(e) => setForm({ ...form, displayName: e.target.value })} required /></div><div className="field"><label htmlFor="organization">机构</label><input id="organization" value={form.organization} onChange={(e) => setForm({ ...form, organization: e.target.value })} /></div><div className="field"><label htmlFor="serviceTags">服务标签（用 | 分隔）</label><input id="serviceTags" value={form.serviceTags} onChange={(e) => setForm({ ...form, serviceTags: e.target.value })} /></div><div className="field"><label htmlFor="region">公开服务地区</label><input id="region" value={form.region} onChange={(e) => setForm({ ...form, region: e.target.value })} /></div>{error ? <div className="notice error full">{error}</div> : null}{saved ? <div className="notice success full">已保存。</div> : null}<div className="form-actions full"><button className="button">保存资料</button></div></form></section> : <div className="notice">只读成员不能维护资料。</div>}</main>;
}
